name: flutter_firebase_codelab
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # MIT
  # Android iOS Linux macOS web Windows
  cupertino_icons: ^1.0.3

  # Apache 2.0
  # Android iOS Linux macOS web Windows
  google_fonts: ^2.0.0

  # BSD
  # Android iOS Linux macOS web Windows
  pedantic: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
